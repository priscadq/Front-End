import React, { Component } from "react";

const { firstName, lastName, dni } = this.props.estudiantes;

function EstudiantesLista({ estudiante }) {
  return (
    <ul>
      <li>{firstName} </li>
    </ul>
  );
}

export default EstudiantesLista;
